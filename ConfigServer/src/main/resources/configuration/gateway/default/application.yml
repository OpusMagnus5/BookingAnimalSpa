server:
  port: 8011

eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8762/eureka/"

spring:
  threads:
    virtual:
      enabled: true
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true